{
  "name": "microfrontend",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "searhcomponent",
    "homepage"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "serve": "npm run preview --workspaces",
    "stop": "kill-port --port 5000,5001"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "*",
    "kill-port": "^2.0.1"
  }
}
